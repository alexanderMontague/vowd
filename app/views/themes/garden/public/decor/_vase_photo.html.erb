<%
  asset = local_assigns[:asset]
  caption = local_assigns[:caption]
  wrapper_class = local_assigns.fetch(:wrapper_class, "")
  slot_key = local_assigns[:slot]
%>
<% inner = capture do %>
  <% if asset.present? %>
    <figure class="garden-planter <%= wrapper_class %>">
      <div class="garden-planter__arch">
        <%= image_tag wedding_asset_url(asset),
            class: "garden-planter__image",
            alt: wedding_asset_alt(asset), loading: "lazy", decoding: "async" %>
      </div>
      <%= theme_ornament("garden", "terracotta-pot", css_class: "garden-planter__pot") %>
      <% if caption.present? %>
        <figcaption class="garden-planter__caption"><%= caption %></figcaption>
      <% end %>
    </figure>
  <% end %>
<% end %>
<% if slot_key.present? %>
  <%= editor_slot(slot_key, css_class: wrapper_class) { inner } %>
<% else %>
  <%= inner %>
<% end %>
